# 第一轮与第二轮对比

- 第一轮：`run-20260902T035556Z-0a43adb5`，创建于 2026-09-02T03:55:56Z
- 第二轮：`run-20260902T125247Z-d8922e23`，创建于 2026-09-02T12:57:05Z，本轮于 2026-09-03 在同一 run ID 内续作完成

第一轮数据全部取自第二轮归档的 `checks/round1_defect_baseline.json`（33933 B，`generated_at = 2026-09-02T13:06:30Z`，读取了 37 个第一轮证据文件）与 `reports/第一轮缺陷基线.md`。第一轮目录下不存在 `final_checkpoint/test_summary.json`，因此该文件无法作为对照源。按隔离约束，本轮未读取第一轮的 `workspace/source/**` 与 `publish/local-product/**`。

## 1. 总体判定

| 维度 | 第一轮 | 第二轮 |
| --- | --- | --- |
| `state.json` / stage 状态 | 19 个 stage 全部 `passed` | 全流程完成 |
| `generated_quality.json` | **`passed = false`**，7 项 required 全失败 | **`passed = true`**，14 项全通过 |
| 浏览器验收 | `not_run`（`required = false`，「未提供浏览器验收执行器」）；run 结束约 2.5 小时后的架构验收失败 | 真实 Chromium 执行，`passed = true`，11 张截图，7 项交互全通过 |
| `support_status` | `inconclusive`（`stop_inconclusive`，置信度 low） | `partially_supported` |
| 致命数据错误 | 1 | 0 |
| stage 与工程质量是否一致 | **背离**：stage 全绿，工程质量全红 | 一致 |
| 归档缺陷 | 12 条（D1–D12，含 5 条 critical） | 见第 4 节逐条处置 |
| 运行期异常 | 15 次配置漂移、18 次 resume、修复预算耗尽 | 一次集中修正，`is_single_batch = true` |

第一轮最严重的问题不是某一项门禁失败，而是**门禁之间不联动**：stage 门禁只看生成契约得分（90/100，阈值 85）与流程性检查，不读取 `generated_quality.json` 的 required 工程检查，因此 19 个 stage 全绿而工程质量 7 项全红，`final_validation` 仍记 `passed`，`publish_web` 仍照常发布。第二轮把 14 项工程门禁与 4 项独立检查全部纳入终态判定，`final_checkpoint` 的 `checks_failed = 0` 才允许定档 `passed`。

## 2. 工程质量量化对比

| 检查 | 第一轮 | 第二轮 | 差值 |
| --- | --- | --- | --- |
| required 检查失败数 | 7 / 7 失败 + 浏览器 `not_run` | 0 / 14 失败 | −7 |
| `pytest:Qwen-Harness` | exit 2；3 collection errors in 0.99s；**0 个测试执行** | exit 0，1.0 s | 收集崩溃 → 全通过 |
| `pytest:evaluation_model_qwen` | exit 2；1 collection error in 0.79s；0 个测试执行；修复后仍 **27 failed / 13 passed** | exit 0，1.0 s | −27 failed |
| `pytest:weather_api_data` | exit 2；2 collection errors in 0.79s；0 个测试执行 | exit 0，0.8 s | 收集崩溃 → 全通过 |
| Ruff | exit 1；**83 errors**（48 可 `--fix`），典型 I001 / TRY004 / E501 / 未使用导入 | 4 个模块各 exit 0，0.1 s 各 | −83 |
| Pyright | exit 1；**279 errors** / 0 warnings / 0 informations，典型 `reportMissingImports(click)`、`reportCallIssue`、`reportAttributeAccessIssue` | 4 个模块各 exit 0 | −279 |
| Node 契约测试 | exit 1；48 tests：**47 pass / 1 fail**（`research-harness-ui.js` 用 `.innerHTML` 赋值动态内容，契约要求 `textContent` / `replaceChildren`） | exit 0，0.9 s | −1 |
| 评价 API 健康检查 | exit 1；`ImportError: cannot import name 'EnvironmentRecord' from 'evaluation_model_qwen.models'`；修复后 health 200 但 POST `/api/v…` 仍异常 | exit 0，1.0 s，`route_count = 90` | 修复 |
| 生成契约总分 | 90 / 100（阈值 85，passed） | — | — |
| 生成契约 environment_interface | **0 / 10，未通过** | 环境契约 54 / 54 格、14 字段、`excluded_fields = []` | 修复 |
| 生成契约 core_interactions | 3 / 4 类交互元素 | 7 项必需交互全部实测通过 | +4 |

第一轮三组 pytest 的共同特征是**在收集阶段就崩溃**（合计 6 个 collection error，0 个测试真正执行），错误全部是导入问题：`No module named 'qwen_harness.experiment'`、`cannot import name 'LockConflictError'`、`cannot import name 'StageStatus'`、`cannot import name 'EnvironmentBlock'`。这说明第一轮生成的代码与测试之间签名分叉，而不是测试用例本身失败。第二轮 4 组 pytest 全部 exit 0，导入契约已对齐。

第一轮 Node 契约测试中，路线数据部分其实是**通过**的：`route_catalog` 与 geojson 的 90 条、30/30/30 模式分布、`route_id` 一致性、徐汇经纬度范围检查全部通过。唯一失败项是前端 `.innerHTML` 用法。第二轮该项 exit 0。

## 3. 科研与数据链路对比

| 指标 | 第一轮 | 第二轮 | 差值 |
| --- | --- | --- | --- |
| 实验单元总数 | 45 | 81 | +36 |
| 就绪单元 | 27 | 81 | +54 |
| `ready_share` | 0.60 | **1.0** | +0.40 |
| `no_candidate` 单元 | **18 / 45** | **0** | −18 |
| 主指标 | Jaccard 无法计算，配对比较全为 0 对或全平局 | 五项指标全部可计算 | 修复 |
| `detour_pass_rate` | — | 0.851852（阈值 ≥ 0.9，**未达标**） | 新增可测量 |
| `environment_win_rate` | — | 0.802469（≥ 0.6，达标） | 新增可测量 |
| `preference_win_rate` | — | 1.0（≥ 0.6，达标） | 新增可测量 |
| `constraint_pass_rate` | — | 1.0（≥ 0.9，达标） | 新增可测量 |
| `mean_detour_ratio` | — | 0.06047（≤ 0.2，达标） | 新增可测量 |
| 致命数据错误 | 1 | 0 | −1 |
| 模块记录契约破损 | **9 条** `module_record_invalid`（P01–P09 的 `input.json` 无法解析为 `ModuleResult`，各 9 个 `extra_forbidden` 校验错误） | 0 | −9 |
| 环境模块预检 | `partial`，**272 条警告**（270 条单位不一致 + 全估算 + 缺 6 个导出快照） | 54 / 54 格，`worst_missing_rate = 0.0`，`excluded_fields = []` | −272 |
| 迭代决策 | `stop_inconclusive`，不进入第二轮迭代（iteration 1/2） | 一次集中修正后完成 | — |

第一轮 18 / 45 单元 `no_candidate` 是 D6（critical）的内容：路线数据在 M1 距离门禁下候选全灭。第二轮 90 条路线全部 9 个「模式 × 距离带」单元各 10 条，`unfilled_slots = []`，因此评价实验的候选供给不再缺口，`ready_share` 从 0.60 升到 1.0。这是第二轮科研结论能够从 `inconclusive` 前进到 `partially_supported` 的直接前提。

## 4. 12 条第一轮缺陷的逐条处置

| ID | 严重度 | 第一轮问题 | 第二轮处置 |
| --- | --- | --- | --- |
| D1 | critical | `generated_quality.json` `passed = false`，7 项 required 全失败 | **已解决**：14 / 14 通过，浏览器验收实际执行 |
| D2 | critical | 三组 pytest 收集阶段崩溃，6 个 collection error，0 测试执行 | **已解决**：4 组 pytest 全部 exit 0 |
| D3 | critical | Ruff 83、Pyright 279、Node 48 测 1 败、评价 API 失败 | **已解决**：全部 exit 0 |
| D4 | critical | 浏览器架构验收失败，desktop 端 route-card testid 缺失 | **已解决**：真实 Chromium 验收 `passed = true`，90 张路线卡片实测渲染 |
| D5 | major | 页面缺少可接受的路线卡交互架构；推荐 API `failed_import` | **已解决**：7 项必需交互全通过，推荐首选实测总分 75.5 |
| D6 | critical | 路线数据在 M1 距离门禁下候选全灭，18 / 45 单元 `no_candidate` | **已解决**：`no_candidate = 0`，9 个单元各 10 条 |
| D7 | major | 徐汇边界贴合率、在区率、道路吸附偏差无量化证据（仅有经纬度包围盒级检查） | **部分解决**：第二轮建立了量化门禁（在区率 ≥ 0.9、吸附率 ≥ 0.98、重叠 ≤ 0.90，`overlap_violations = 0`），但第一轮没有留下对应数字，**无法给出真实差值**，`route_geometry_defects` 6 条全部记为 `evidence_not_found_in_allowed_artifacts` |
| D8 | critical | 环境模块预检 `partial`，272 条警告 | **已解决**：54 / 54 格，14 字段，`worst_missing_rate = 0.0`，`excluded_fields = []` |
| D9 | major | 科研结论 `inconclusive`，主指标 Jaccard 无法计算，配对比较全为 0 对或全平局 | **部分解决**：升到 `partially_supported`，五项指标可计算，但 `detour_pass_rate = 0.851852 < 0.9` 未达标 |
| D10 | critical | stage 全绿与工程质量全红背离；15 次配置漂移、18 次 resume、修复预算耗尽 | **已解决**：终态定档以门禁实测为准，一次集中修正完成，`checks_failed = 0` |
| D11 | major | 测试与命令解析到仓库根目录而非 run 工作区，环境隔离契约失效 | **已解决**：全部命令 `cwd = RUN/workspace/source`，子进程环境剥离敏感键，`check_output_boundary` `status_lines = 1`（仓库根 `.vscode/settings.json`，白名单内） |
| D12 | major | 模块结果记录契约破损，9 个画像输入 JSON 均无法解析为 `ModuleResult` | **已解决**：`module_record_invalid = 0`，`fatal_data_error_count = 0` |

统计：已解决 10 条，部分解决 2 条（D7、D9），未解决 0 条。5 条 critical 全部解决。

## 5. 路线组合对比

| 项目 | 第一轮 | 第二轮（盲测） | 第二轮（集中修正后） |
| --- | --- | --- | --- |
| 路线总数 | 90（catalog 通过契约检查） | **87** | **90** |
| bike band 2 | — | **7** | **10** |
| 9 个分带单元 | 未建立分带门禁 | 3 个单元不满 | 全部各 10 |
| `kind_swaps` | — | 存在降级 | `[]` |
| `unfilled_slots` | — | 3 | `[]` |
| 每模式闭环 / 单程 | — | 不均衡 | 15 / 15 |
| 空间门禁 | 仅经纬度包围盒级检查 | `route_spatial_quality` **failed** | `PASSED`，`overlap_violations = 0` |
| 区域覆盖 | — | — | 龙华 23、衡复 18、康健园 11、华泾 11、植物园 9、西岸 8、漕河泾 6、徐家汇 4 |

值得注意：第一轮 catalog 的 90 条路线在 Node 契约测试中是通过的（90 条、30/30/30、`route_id` 一致、经纬度范围内），但第一轮**没有**分带单元门禁、没有在区率与吸附率量化门禁、没有同模式重叠上限。第二轮补齐这些门禁后，盲测阶段自己的 87 条立刻被判负——这正是第二轮比第一轮严格的地方，也是 D7 的正面回应。

## 6. 产品与交付对比

| 项目 | 第一轮 | 第二轮 |
| --- | --- | --- |
| 12 项可见产品矩阵 | 未建立 | **12 / 12**（盲测 11 / 12） |
| 桌面视口验收 | 架构验收失败 | 1440×900，7 张截图，passed |
| 移动视口验收 | 未执行 | 500×700，4 张截图，passed |
| 本地产品访问 | `http://127.0.0.1:8130/web/` | `http://127.0.0.1:8765/index.html`（`powershell -File publish/launch-local.ps1`） |
| 发布产物与源码一致性 | 未校验 | 4 个前端文件 SHA-256 逐一比对全部相同 |
| payload | — | 16246288 B，`missing_inputs = []`，`partial_data = false` |
| 边界与密钥 | — | `check_output_boundary` PASSED，`check_secret_scan` 455 文件 0 违规 |

## 7. 模型通道对比

两轮的 provider 与计费通道一致：`qoder_session` / `qwen3.8-max` / `qoder_credits`，`dashscope_api_used = false`。第二轮额外落盘了 `evidence/model_channel.json` 与 `evidence/user_screenshot_checklist.md`，并通过 `check_secret_scan`（`env_files = 0`）与子进程环境剥离（`sensitive_env_keys_removed_in_subprocess = true`）证明没有读取或泄漏密钥。

两轮都未完成 Qoder Credits 消耗量与百炼调用量的用户截图，状态均为 `evidence_pending_user_capture`。这不是本会话可自证的量，需要用户在客户端手工截取。

## 8. 结论

第二轮相对第一轮的实质改进集中在三点：

1. **工程门禁从全红到全绿。** Ruff −83、Pyright −279、pytest 从 0 测试执行到 4 组全通过、Node 契约 −1、评价 API 从 `ImportError` 到 `route_count = 90`。
2. **科研链路从数据缺口到满供。** `no_candidate` 18 → 0，`ready_share` 0.60 → 1.0，致命数据错误 1 → 0，环境预检警告 272 → 0，`support_status` `inconclusive` → `partially_supported`。
3. **判定口径从流程绿灯改为门禁实测。** 这是 D10 的根因治理：终态不再由 stage 是否走完决定，而由 14 项强制门禁 + 4 项独立检查 + 浏览器实测证据决定。

未消除的两项：`detour_pass_rate = 0.851852` 低于 0.9（D9 只解决了一半），以及第一轮几何质量数字缺失导致 D7 无法给出真实差值。两者都如实记录，未通过调整判定口径掩盖。
