# 徐汇路线构建器优化方案

## Summary

把 `xuhui_route_builder` 从静态样例地图升级为“高德底图 + 徐汇真实边界 + 150 条候选运动路线 + 搜索后展示 + 起终点导航”的原型。当前只保留一套高德地图实现，不做 Leaflet/高德双栈兼容。

当前已确认的问题：

- `data/web/xuhui_routes.geojson` 只有 3 条路线，且 `source_method=manual_sample`，坐标点直接连线，所以出现路线穿楼、穿江、未贴合道路。
- `data/web/xuhui_boundary.geojson` 是手工矩形，无法代表严格徐汇区边界。
- `main.js` 启动时直接调用 `drawEntries` 和 `drawRoutes`，导致所有点和路线默认铺满地图。
- “桂江路中环绿廊入口 康健园 community_node” 是入口池样例点，概念上有用，但现在缺少图层开关和解释，视觉上像孤立脏点。
- UI 目前只有筛选列表，没有起点、终点、运动方式、途经偏好、候选路线、接驳路径这些导航核心控件。

## Key Changes

- 地图底座：改用高德 JS API 2.0 默认矢量底图，理由是上海中文 POI、道路层级、搜索和路径规划展示更贴近目标用户；保留现有 GeoJSON 数据格式作为后端导出格式。
- 徐汇边界：用高德行政区域查询 `adcode=310104` 或 `keywords=徐汇区` 获取真实 boundary polyline，导出为 `xuhui_boundary.geojson`，替换矩形。
- 路线库：从 15 条 seed 扩展到 150 条候选路线，按徐汇滨江、上海植物园、康健园、徐家汇、龙华、衡复、漕河泾 7 个片区组织；来源母线包括徐汇滨江跑步、衡复 Citywalk、上海植物园、桂江路中环绿廊、徐汇滨江骑行等公开路线，再通过入口、途经点、距离档生成候选。
- 路线拟合：跑步优先用高德步行路径生成 polyline，骑行用高德骑行路径，跑步视为步行路网上的运动模式；路线质量过滤包含距离误差、区内比例、转向数、路线重复率和是否穿越不可达区域。
- 展示逻辑：页面初始只显示高德底图、徐汇边界和必要搜索控件；用户搜索路线、输入起终点或选择片区后，才绘制匹配路线和入口点。
- 导航能力：新增起点、终点、运动方式、目标距离、途经偏好控件；途经偏好第一版支持咖啡店、厕所、便利店、地铁站、公园入口；输出推荐路线、便捷路线和候选路线。
- 点位逻辑：入口点分为 `metro_exit`、`park_gate`、`riverside_access`、`community_node`、`office_cluster`、`scenic_node` 等，只在搜索结果或图层开关打开时显示；`community_node` 保留用于社区出发和康健短跑，但默认隐藏。
- UI 方向：做成“安静、专业、地图优先”的工具界面，左侧为紧凑规划面板，地图占主视觉；用颜色区分跑步、步行、骑行、接驳段，避免营销式大卡片。

## Implementation Changes

- Python 数据层：
  - 扩展 `AmapClient` 到高德 v5 POI 和路径规划 2.0，补齐行政边界、地理编码、POI 搜索、步行、骑行请求。
  - 新增路线生成命令：读取 seed、入口池、途经点池，批量生成 150 条路线并缓存原始响应。
  - 导出 `xuhui_boundary.geojson`、`xuhui_entries.geojson`、`xuhui_routes.geojson`、`route_catalog.json`、`poi_catalog.json` 和 `access_cases.json`。

- Web 交互层：
  - 替换 `map.js` 的 Leaflet 初始化为高德地图初始化。
  - 重构 `main.js` 为状态驱动：初始态、搜索态、路线结果态、导航结果态。
  - 重构 `route-ui.js`：增加起终点输入、运动方式、距离档、途经偏好、结果排序和详情面板。
  - 样式重做为地图应用布局，避免现在的白底列表式粗糙感。

- 数据来源：
  - 高德 JS API 2.0：https://lbs.amap.com/api/javascript-api-v2/summary
  - 高德路径规划：https://lbs.amap.com/api/webservice/guide/api/newroute
  - 高德行政区域查询：https://lbs.amap.com/api/webservice/guide/api/district
  - 高德 POI 搜索：https://lbs.amap.com/api/webservice/guide/api-advanced/newpoisearch
  - 徐汇滨江跑步路线：https://www.meet-in-shanghai.net/tc/news/sports-paradise-beautiful-riverside-artistic-shoreline-come-and-run-along-the-xuhui-riverside-514030/
  - 桂江路中环绿廊：https://www.thepaper.cn/newsDetail_forward_23437827
  - 徐汇滨江骑行线：https://wap.xinmin.cn/content/32386375.html
  - 衡复 Citywalk：https://www.xuhui.gov.cn/xwzx_zwxx/20250211/551975.html

## Test Plan

- 数据测试：150 条路线数量达标；路线 ID 唯一；每条路线有 polyline、距离、时长、片区、运动方式、来源和置信度。
- 空间测试：徐汇边界不是矩形；路线 `route_inside_ratio` 大于 0.80；入口点落在徐汇区或边界合理缓冲区内。
- 质量测试：跑步/步行路线距离误差控制在目标距离 15% 内；骑行路线距离误差控制在 20% 内；重复路线按重合度剔除。
- UI 测试：初始地图不显示全部 150 条路线；搜索后只显示命中的路线；起点终点输入后能画出候选路径并显示距离和时间。
- 导航测试：徐家汇站到徐家汇公园、龙耀路站到徐汇滨江、漕河泾办公点到康健园、衡山路到武康路、植物园入口闭环这些样例都能返回可视路线。
- 回归测试：现有 `pytest` 通过；新增边界解析、路线 schema、POI schema、导航结果 schema、前端静态文件检查。

## Assumptions

- 正式实施时需要可用的 `AMAP_WEB_SERVICE_KEY` 和 `AMAP_JS_API_KEY`；密钥只放本地 `.env` 或本地配置文件，不进入仓库。
- 第一版导航使用高德路径规划结果，不自建 OSRM、GraphHopper 或 pgRouting。
- 第一版“跑步”按步行路网生成，靠标签、距离和场景区分，不额外引入跑步专用路由引擎。
- 第一版重点做地图、路线、导航和候选库，PM2.5、噪声、花粉综合评分只保留字段入口。
- 正式实施时按你指定的方式使用多个子 agent，并统一使用 gpt-5.5high。
